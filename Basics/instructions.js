/*

~ Run following commands first
 npm i -g typescript
 npm i -g ts-node --save-dev

~ Create a tsconfig.json file and write following lines into it

*{
*  "compilerOptions": {
*       "strictPropertyInitialization": false,
*   }
* }

~ Now you can directly run your TS file without having to compile it

~ To compile a ts file, run `tsc filename.ts`. It will be compiled into js file.

*/